# PC-PLC 通信协议

**版本**: Rev1.10

## 通信格式

| 参数 | 说明 |
|------|------|
| **硬件方式** | 网口 |
| **协议** | MODBUS TCP |
| **网段** | 192.168.1.### |
| **PLC IP** | 192.168.1.100 |
| **端口号** | 502 |
| **主从站分配** | 上位机做主站，PLC做从站，地址为:1 |

## Modbus 地址映射表

### 上位机读数据（只读）

#### 交互类 - 位置寄存器

| 功能描述 | 数据类型 | Modbus地址 | 精度 | 备注 |
|----------|----------|-----------|------|------|
| 1#X轴当前位置 | 寄存器 (DInt) | 2000 | 1=0.01mm | 双字整型 |
| 2#X轴当前位置 | 寄存器 (DInt) | 2002 | 1=0.01mm | 双字整型 |
| 旋转轴当前位置 | 寄存器 (DInt) | 2004 | 1=0.01° | 双字整型 |

#### 交互类 - 探头测量数据寄存器

| 功能描述 | 数据类型 | Modbus地址 | 精度 | 备注 |
|----------|----------|-----------|------|------|
| 1#测距数值 | 寄存器 (DInt) | 110 | 1=0.01mm | 双字整型 |
| 2#测距数值 | 寄存器 (DInt) | 112 | 1=0.01mm | 双字整型 |

#### 交互类 - 状态线圈

| 功能描述 | 数据类型 | Modbus地址 | 备注 |
|----------|----------|-----------|------|
| 1#X轴初始化完成 | 线圈 (Bool) | 900 | - |
| 2#X轴初始化完成 | 线圈 (Bool) | 901 | - |
| 旋转轴初始化完成 | 线圈 (Bool) | 902 | - |
| 1#X轴初始化中 | 线圈 (Bool) | 1003 | - |
| 2#X轴初始化中 | 线圈 (Bool) | 1103 | - |
| 旋转轴初始化中 | 线圈 (Bool) | 1203 | - |
| 系统初始化中 | 线圈 (Bool) | 700 | - |
| 系统初始化完成 | 线圈 (Bool) | 701 | - |
| 急停按下 | 线圈 (Bool) | 710 | - |
| 1#X轴报警 | 线圈 (Bool) | 711 | - |
| 2#X轴报警 | 线圈 (Bool) | 712 | - |
| 旋转轴报警 | 线圈 (Bool) | 713 | - |
| 1#X轴动作中 | 线圈 (Bool) | 720 | - |
| 2#X轴动作中 | 线圈 (Bool) | 721 | - |
| 旋转轴动作中 | 线圈 (Bool) | 722 | - |

### 上位机读写数据（读写）

#### 运动控制线圈

| 功能描述 | 数据类型 | Modbus地址 | 备注 |
|----------|----------|-----------|------|
| 1#X轴点动前进 | 线圈 (Bool) | 1000 | - |
| 1#X轴点动后退 | 线圈 (Bool) | 1001 | - |
| 1#X轴初始化 | 线圈 (Bool) | 1002 | - |
| 1#X轴动作 | 线圈 (Bool) | 1010 | - |
| 2#X轴点动前进 | 线圈 (Bool) | 1100 | - |
| 2#X轴点动后退 | 线圈 (Bool) | 1101 | - |
| 2#X轴初始化 | 线圈 (Bool) | 1102 | - |
| 2#X轴动作 | 线圈 (Bool) | 1110 | - |
| 旋转轴点动前进 | 线圈 (Bool) | 1200 | - |
| 旋转轴点动后退 | 线圈 (Bool) | 1201 | - |
| 旋转轴初始化 | 线圈 (Bool) | 1202 | - |
| 旋转轴动作 | 线圈 (Bool) | 1210 | - |
| 急停 | 线圈 (Bool) | 249 | - |
| 整机初始化 | 线圈 (Bool) | 104 | - |

#### 运动参数寄存器

| 功能描述 | 数据类型 | Modbus地址 | 备注 |
|----------|----------|-----------|------|
| 1#X轴动作位置 | DInt | 41212 | - |
| 2#X轴动作位置 | DInt | 41312 | - |
| 旋转轴动作位置 | DInt | 41412 | - |
| 1#X轴点动速度 | DInt | 41188 | - |
| 2#X轴点动速度 | DInt | 41288 | - |
| 1#X轴动作速度 | DInt | 41190 | - |
| 2#X轴动作速度 | DInt | 41290 | - |
| 旋转轴动作速度 | DInt | 41390 | - |

### PC 状态监控

| 功能描述 | 数据类型 | Modbus地址 | 备注 |
|----------|----------|-----------|------|
| 上位机心跳信号 | Int | 2200 | 上位机写入"0/1"交替；PLC只读（16位） |
| 反馈心跳信号 | Int | 2202 | PLC写入"0/1"交替；视觉只读（16位） |

## 数据类型说明

| 类型 | 字长 | 说明 |
|------|------|------|
| Bool | 1 bit | 布尔型（线圈） |
| Int | 16 bit | 有符号整数（寄存器） |
| DInt | 32 bit | 双字整数（占用2个连续寄存器） |

## 使用示例

### 读取位置数据

```python
# 读取 1#X轴当前位置 (寄存器 2000, DInt 类型)
result = client.read_holding_registers(2000, 2, slave=1)
position_raw = (result.registers[0] << 16) | result.registers[1]
position_mm = position_raw * 0.01  # 精度转换
```

### 读取探头测量数据

```python
# 读取 1#测距数值 (寄存器 110, DInt 类型, 精度 0.01mm)
result = client.read_holding_registers(110, 2, slave=1)
probe1_raw = (result.registers[0]) | (result.registers[1] << 16)  # Little Endian
probe1_mm = probe1_raw * 0.01  # 转换为 mm

# 读取 2#测距数值 (寄存器 112)
result = client.read_holding_registers(112, 2, slave=1)
probe2_raw = (result.registers[0]) | (result.registers[1] << 16)
probe2_mm = probe2_raw * 0.01
```

### 控制轴运动

```python
# 写入 1#X轴动作位置 (寄存器 41212)
target_position = int(50.0 / 0.01)  # 50mm
client.write_registers(41212, [target_position >> 16, target_position & 0xFFFF], slave=1)

# 启动运动 (线圈 1010)
client.write_coil(1010, True, slave=1)
```

### 初始化系统

```python
# 整机初始化 (线圈 104)
client.write_coil(104, True, slave=1)

# 等待初始化完成 (线圈 701)
while True:
    result = client.read_coils(701, 1, slave=1)
    if result.bits[0]:
        break
    time.sleep(0.1)
```

## 注意事项

1. **地址偏移**: Modbus 协议中寄存器地址需要减1（如协议中的2000，实际读取时为1999）
2. **字节序**: DInt 类型占用2个连续寄存器，高位在前
3. **从站地址**: 所有通信必须指定 `slave=1`
4. **心跳信号**: 建议每秒更新一次，用于检测通信是否正常
5. **精度转换**: 位置寄存器的值需要乘以 0.01 才能得到实际的 mm 值

## 手动操作说明

### 点动操作
点动模式用于手动微调位置，操作时需按住按钮：
- 按住"前进/后退"按钮，轴持续运动
- 松开按钮，轴立即停止
- 点动速度可实时调整（建议范围: X轴 10-1000，旋转轴 5-200）

### 定位动作
定位动作用于自动移动到指定位置：
1. 设置目标位置
2. 点击"定位"按钮
3. 轴自动移动到目标位置并停止
4. 动作速度通常为点动速度的2倍

### 回原点
- 点击"回原点"按钮，轴自动返回原点（Home位置）
- 1#X轴原点: -700mm
- 2#X轴原点: +700mm  
- 旋转轴原点: 0°

### 整机初始化
- 执行所有轴的回原点操作
- 清除报警状态
- 重置系统状态
